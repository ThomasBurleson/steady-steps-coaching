# Dependencies
node_modules/

# Build output
dist/

# Generated responsive hero variants (deterministic; built from the committed
# master src/_images/forest.avif via `npm run images`, run by predev + build).
public/forest-*.avif

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment overrides (keep secrets out of git; .env.example is committed)
.env
.env.local
.env.*.local
!.env.example

# OS artifacts
.DS_Store

# Blog-convert scratch (temp .docx/.md; markitdown can't read /tmp — see blog-convert skill)
.blog-tmp/
